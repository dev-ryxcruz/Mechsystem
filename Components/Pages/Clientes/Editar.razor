@page "/clientes/editar/{Id:int}"
@rendermode InteractiveServer

@using mechsystem.Models
@using mechsystem.Interfaces
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Roles = "Administrador, Atendimento")]

@inject IClienteRepository ClienteRepository
@inject NavigationManager NavigationManager

<div class="container mt-5">
    <div class="card shadow-sm">
        <div class="card-header bg-warning text-dark">
            <h4 class="mb-0">Editar Cliente</h4>
        </div>
        <div class="card-body">
            
            <EditForm Model="@cliente" OnValidSubmit="GuardarEdicao">
                <DataAnnotationsValidator />

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label class="form-label">Nome Completo</label>
                        <InputText @bind-Value="cliente.Nome" class="form-control" />
                        <ValidationMessage For="@(() => cliente.Nome)" class="text-danger" />
                    </div>

                    <div class="col-md-6 mb-3">
                        <label class="form-label">CPF</label>
                        <InputText @bind-Value="cliente.Cpf" class="form-control" />
                        <ValidationMessage For="@(() => cliente.Cpf)" class="text-danger" />
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label class="form-label">E-mail</label>
                        <InputText @bind-Value="cliente.Email" class="form-control" />
                    </div>

                    <div class="col-md-6 mb-3">
                        <label class="form-label">Telefone</label>
                        <InputText @bind-Value="cliente.Telefone" class="form-control" />
                    </div>
                </div>

                <div class="mb-3">
                    <label class="form-label">Endereço</label>
                    <InputText @bind-Value="cliente.Endereco" class="form-control" />
                </div>

                <div class="mt-4">
                    <button type="submit" class="btn btn-warning">
                        <i class="bi bi-save"></i> Guardar Alterações
                    </button>
                    <button type="button" class="btn btn-secondary" @onclick="Cancelar">
                        Cancelar
                    </button>
                </div>
            </EditForm>

        </div>
    </div>
</div>

@code {
    // Este Parâmetro capta o {Id} que vem na barra de endereços do navegador
    [Parameter]
    public int Id { get; set; }

    private Cliente cliente = new Cliente();

    // Assim que o ecrã abre, ele vai buscar os dados antigos do cliente à base de dados
    protected override async Task OnInitializedAsync()
    {
        var clienteEncontrado = await ClienteRepository.BuscarPorId(Id);
        if (clienteEncontrado != null)
        {
            cliente = clienteEncontrado;
        }
    }

    // Quando clicamos no botão amarelo, ele atualiza no SQLite
    private async Task GuardarEdicao()
    {
        await ClienteRepository.Atualizar(cliente);
        NavigationManager.NavigateTo("/clientes");
    }

    private void Cancelar()
    {
        NavigationManager.NavigateTo("/clientes");
    }
}